% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmedsem_csem.R
\name{rmedsem.cSEMResults}
\alias{rmedsem.cSEMResults}
\title{Mediation Analysis for cSEM Models}
\usage{
\method{rmedsem}{cSEMResults}(
  mod,
  indep,
  med,
  dep,
  approach = c("bk", "zlc"),
  p.threshold = 0.05,
  effect.size = c("RIT", "RID", "upsilon"),
  nbootstrap = 1000,
  ci.two.tailed = 0.95,
  ...
)
}
\arguments{
\item{mod}{A fitted SEM model (cSEM).}

\item{indep}{A string indicating the name of the independent variable in the model.}

\item{med}{A string indicating the name of the mediator variable in the model.}

\item{dep}{A string indicating the name of the dependent variable in the model.}

\item{approach}{either 'bk' or 'zlc' or both c("bk", "zlc") (default)}

\item{p.threshold}{A double giving the p-value for determining whether a path
is significant or not}

\item{effect.size}{calculate different effect-sizes; one or more of "RIT", "RID"}

\item{nbootstrap}{number of bootstrap samples (integer >= 2), default=1000}

\item{ci.two.tailed}{A double giving the confidence level for two-tailed confidence intervals (default 0.95)}

\item{...}{additional arguments (currently unused)}
}
\value{
A \code{rmedsem} structure containing the results from the analysis
}
\description{
Mediation Analysis for cSEM Models
}
\examples{
\donttest{
if (requireNamespace("cSEM", quietly = TRUE)) {
  model <- "
    # measurement model
    OwnLook  =~ smv_attr_face + smv_attr_body + smv_sexy
    SelfEst  =~ ses_satis + ses_qualities + ses_able_todo
    MentWell =~ mwb_optimistic + mwb_useful + mwb_energy
    # structural model
    SelfEst  ~ OwnLook
    MentWell ~ OwnLook + SelfEst
  "
  mod <- cSEM::csem(rmedsem::mchoice, model)
  # small number of bootstrap samples to keep the example fast
  out <- rmedsem(mod, indep="OwnLook", med="SelfEst", dep="MentWell",
                 nbootstrap=200)
  print(out)
}
}
}
