% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R, R/plotting.R
\name{plot.rmedsem}
\alias{plot.rmedsem}
\alias{plot_effect}
\alias{plot_coef}
\title{Plot rmedsem Results}
\usage{
\method{plot}{rmedsem}(x, type = c("coef", "effect"), ...)

plot_effect(res, description = TRUE)

plot_coef(res)
}
\arguments{
\item{x, res}{an \code{rmedsem} object}

\item{type}{character; \code{"coef"} (default) for a coefficient plot or
\code{"effect"} for an effect size plot}

\item{...}{additional arguments passed to \code{plot_coef()} or \code{plot_effect()}}

\item{description}{logical, whether to add a caption describing the
proportion of the total effect that is mediated (default \code{TRUE})}
}
\value{
a \code{ggplot} object
}
\description{
Visualize the results of \code{\link[=rmedsem]{rmedsem()}}.
}
\details{
\describe{
\item{\code{plot_coef()}}{A coefficient plot of the indirect effect (for each
estimation method), the direct effect and the total effect, with their
confidence (or credible) intervals.}
\item{\code{plot_effect()}}{A pie chart of the (absolute) indirect and direct
effects, i.e., the proportion of the total effect that is mediated.
Requires the effect sizes \code{"RIT"} and \code{"RID"}.}
}
\code{plot()} calls \code{plot_coef()} (\code{type = "coef"}) or \code{plot_effect()}
(\code{type = "effect"}).
}
\examples{
mod.txt <- "
read ~ math
science ~ read + math
"
mod <- lavaan::sem(mod.txt, data=rmedsem::hsbdemo)
out <- rmedsem(mod, indep="math", med="read", dep="science")
plot(out)
plot(out, type="effect")
plot_effect(out, description=FALSE)

}
\seealso{
\code{\link[=rmedsem]{rmedsem()}}, \link{rmedsem-methods}
}
