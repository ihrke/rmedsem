% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmedsem.R, R/rmedsem_blavaan.R,
%   R/rmedsem_csem.R, R/rmedsem_lavaan.R, R/rmedsem_modsem.R
\name{rmedsem}
\alias{rmedsem}
\alias{rmedsem.default}
\alias{rmedsem.blavaan}
\alias{rmedsem.cSEMResults}
\alias{rmedsem.lavaan}
\alias{rmedsem.modsem}
\title{Mediation Analysis for Structural Equation Models}
\usage{
rmedsem(
  mod,
  indep,
  med,
  dep,
  approach = c("bk", "zlc"),
  p.threshold = 0.05,
  effect.size = c("RIT", "RID", "upsilon"),
  ...
)

\method{rmedsem}{default}(mod, indep, med, dep, ...)

\method{rmedsem}{blavaan}(
  mod,
  indep,
  med,
  dep,
  approach = c("bk", "zlc"),
  p.threshold = 0.05,
  effect.size = c("RIT", "RID", "upsilon"),
  ci.two.tailed = 0.95,
  hdi = FALSE,
  ...
)

\method{rmedsem}{cSEMResults}(
  mod,
  indep,
  med,
  dep,
  approach = c("bk", "zlc"),
  p.threshold = 0.05,
  effect.size = c("RIT", "RID", "upsilon"),
  nbootstrap = 1000,
  ci.two.tailed = 0.95,
  seed = NULL,
  ...
)

\method{rmedsem}{lavaan}(
  mod,
  indep,
  med,
  dep,
  approach = c("bk", "zlc"),
  p.threshold = 0.05,
  effect.size = c("RIT", "RID", "upsilon"),
  standardized = TRUE,
  mcreps = 5000,
  ci.two.tailed = 0.95,
  ...
)

\method{rmedsem}{modsem}(
  mod,
  indep,
  med,
  dep,
  approach = c("bk", "zlc"),
  p.threshold = 0.05,
  effect.size = c("RIT", "RID", "upsilon"),
  moderator = NULL,
  standardized = TRUE,
  mcreps = 5000,
  ci.two.tailed = 0.95,
  ...
)
}
\arguments{
\item{mod}{a fitted SEM: an object of class \code{lavaan}, \code{cSEMResults},
\code{blavaan} or \code{modsem}. \code{blavaan} models containing latent variables must
be fitted with \code{save.lvs = TRUE}.}

\item{indep}{a string, the name of the independent variable (X). For
\code{modsem} models, this can be an interaction term such as \code{"W:X"}.}

\item{med}{a string, the name of the mediator (M)}

\item{dep}{a string, the name of the dependent variable (Y)}

\item{approach}{approach(es) to determine the type of mediation: \code{"bk"}
(Baron and Kenny), \code{"zlc"} (Zhao, Lynch & Chen), or both (default).
Ignored for \code{blavaan} models.}

\item{p.threshold}{a number between 0 and 1, the p-value threshold for
significance (default 0.05). A p-value equal to the threshold counts as
not significant.}

\item{effect.size}{character vector with the effect sizes to compute; one
or more of \code{"RIT"} (ratio of indirect to total effect), \code{"RID"} (ratio
of indirect to direct effect) and \code{"upsilon"} (Lachowicz et al., 2018);
see \link{effect-sizes}.}

\item{...}{additional arguments passed to methods (currently unused)}

\item{ci.two.tailed}{a number between 0 and 1, the level of all confidence
(or, for \code{blavaan}, credible) intervals (default 0.95)}

\item{hdi}{(\code{blavaan}) a logical. If \code{FALSE} (default), equal-tailed
credible intervals are computed; if \code{TRUE}, highest density intervals
(requires the \pkg{HDInterval} package). Applies to the indirect, direct
and total effects and to Upsilon.}

\item{nbootstrap}{(\code{cSEM}) the number of bootstrap samples (default 1000)}

\item{seed}{(\code{cSEM}) \code{NULL} (default) or a non-negative integer, the seed
for the bootstrap. If \code{NULL}, the seed is drawn from R's random number
generator, so that results can be reproduced with \code{\link[=set.seed]{set.seed()}}.}

\item{standardized}{(\code{lavaan}, \code{modsem}) a logical, whether to use
standardized coefficients (default \code{TRUE}). \code{cSEM} and \code{blavaan} results
are always standardized.}

\item{mcreps}{(\code{lavaan}, \code{modsem}) the number of Monte-Carlo samples, a
positive integer (default 5000)}

\item{moderator}{(\code{modsem}) \code{NULL} (default) or a string, the name of the
moderator W for moderated mediation. The model must contain an
interaction of the moderator with \code{indep} and/or \code{med}.}
}
\value{
an object of class \code{c("rmedsem_<pkg>", "rmedsem")}, where \verb{<pkg>}
identifies the backend (\code{lavaan}, \code{cSEM}, \code{blavaan} or \code{modsem}). See
\link{rmedsem-methods} for functions to print, summarize and extract results,
\link{effect-sizes} for effect sizes and \code{\link[=plot.rmedsem]{plot.rmedsem()}} for plots. The
structure of the object is described in section 'Adding a backend'.
}
\description{
Tests the indirect effect of an independent variable X on a dependent
variable Y through a mediator M in a fitted structural equation model
(SEM), and determines the type of mediation using the Baron and Kenny
(1986) and/or Zhao, Lynch & Chen (2010) approaches. Models estimated with
\pkg{lavaan} (covariance-based SEM), \pkg{cSEM} (PLS-SEM), \pkg{blavaan}
(Bayesian SEM) and \pkg{modsem} (models with latent interactions) are
supported. The model must contain the regression paths X -> M, M -> Y and
X -> Y.
}
\section{Backends}{

\describe{
\item{\code{lavaan}}{The indirect effect is tested with the Sobel, Delta and
Monte-Carlo methods. The Zhao, Lynch & Chen approach is based on the
Monte-Carlo test.}
\item{\code{cSEMResults}}{The model is re-estimated with \code{nbootstrap}
bootstrap samples. The indirect effect is tested with the Sobel, Delta
and bootstrap methods, and the Zhao, Lynch & Chen approach is based on
the bootstrap test. Only single-group, first-order models are supported.}
\item{\code{blavaan}}{Estimates are based on the (standardized) posterior
samples. The output reports posterior means, standard deviations,
posterior probabilities of a positive and negative indirect effect,
evidence ratios and credible intervals; the Baron and Kenny and Zhao,
Lynch & Chen approaches are not applied.}
\item{\code{modsem}}{As for \code{lavaan}. In addition, moderated mediation (via
\code{moderator}) and mediated moderation (an interaction term as \code{indep})
are supported.}
}
Multi-group and multilevel models are not supported.
}

\section{Adding a backend}{

Support for further model classes is added by writing a method
\verb{rmedsem.<class>()} that returns a list of class
\code{c("rmedsem_<pkg>", "rmedsem")}. For the default \code{\link[=print.rmedsem]{print.rmedsem()}},
\code{\link[=plot.rmedsem]{plot.rmedsem()}} and \code{\link[=as.data.frame.rmedsem]{as.data.frame.rmedsem()}} methods to work, the list
must contain the following elements:
\describe{
\item{\code{package}}{name of the estimating package (character).}
\item{\code{standardized}}{whether the coefficients are standardized (logical).}
\item{\code{vars}}{list with elements \code{indep}, \code{med} and \code{dep}.}
\item{\code{est.methods}}{character vector naming the estimation methods for
the indirect effect, e.g. \code{c("sobel", "delta", "montc")}.}
\item{one element per entry in \code{est.methods}}{a named numeric vector with
elements \code{coef}, \code{se}, \code{zval}, \code{pval}, \code{lower} and \code{upper}. The Baron
and Kenny approach requires the element \code{sobel}.}
\item{\code{zlc.method}}{(optional) the entry of \code{est.methods} whose p-value
is used for the Zhao, Lynch & Chen approach; defaults to the last
entry of \code{est.methods}.}
\item{\code{direct.effect}}{named numeric vector with elements \code{coef}, \code{se},
\code{pval}, \code{lower} and \code{upper}.}
\item{\code{total.effect}}{named numeric vector with elements \code{coef}, \code{se},
\code{lower} and \code{upper}.}
\item{\code{med.approach}}{character vector, a subset of \code{c("bk", "zlc")}.}
\item{\code{med.data}}{list with elements \code{sig_thresh} (the p-value
threshold), \code{coefs} and \code{pvals}; the latter two are lists with elements
\code{moi} (X -> M), \code{dom} (M -> Y) and \code{doi} (X -> Y).}
\item{\code{effect.size}}{list with (a subset of) elements \code{RIT}, \code{RID} and
\code{upsilon}, as returned by the built-in backends.}
\item{\code{nobs}}{(optional) number of observations, used by \code{\link[stats:nobs]{stats::nobs()}}.}
\item{\code{ci.level}}{(optional) level of the stored intervals, used by
\code{\link[stats:confint]{stats::confint()}} and \code{\link[=summary]{summary()}}; defaults to 0.95.}
\item{\code{ci.type}}{(optional) label of the stored intervals, \code{"CI"}
(default) or \code{"HDI"}.}
}
A backend whose output does not fit this scheme can provide its own
\verb{print.rmedsem_<pkg>()} method, either replacing the default output (as
for \code{blavaan}) or extending it via \code{\link[=NextMethod]{NextMethod()}} (as for \code{modsem}).
}

\examples{
## lavaan: observed variables
mod.txt <- "
read ~ math
science ~ read + math
"
mod <- lavaan::sem(mod.txt, data = rmedsem::hsbdemo)
out <- rmedsem(mod, indep = "math", med = "read", dep = "science")
out

# Zhao, Lynch & Chen approach only, unstandardized coefficients
rmedsem(mod, indep = "math", med = "read", dep = "science",
        approach = "zlc", standardized = FALSE, mcreps = 5000)

\donttest{
## cSEM
if (requireNamespace("cSEM", quietly = TRUE)) {
  model <- "
    OwnLook  =~ smv_attr_face + smv_attr_body + smv_sexy
    SelfEst  =~ ses_satis + ses_qualities + ses_able_todo
    MentWell =~ mwb_optimistic + mwb_useful + mwb_energy
    SelfEst  ~ OwnLook
    MentWell ~ OwnLook + SelfEst
  "
  mod <- cSEM::csem(rmedsem::mchoice, model)
  # small number of bootstrap samples to keep the example fast
  rmedsem(mod, indep = "OwnLook", med = "SelfEst", dep = "MentWell",
          nbootstrap = 200)
}

## modsem: mediated moderation and moderated mediation
if (requireNamespace("modsem", quietly = TRUE)) {
  m <- "
    OwnLook =~ smv_attr_face + smv_attr_body + smv_sexy
    SelfEst =~ ses_satis + ses_qualities + ses_able_todo
    MentWell =~ mwb_optimistic + mwb_useful + mwb_energy
    smv =~ smv_kind + smv_caring + smv_understanding +
      smv_make_laughh + smv_funny + smv_sociable
    SelfEst ~ OwnLook + smv + smv:OwnLook
    MentWell ~ OwnLook + SelfEst + smv + smv:OwnLook
  "
  est <- modsem::modsem(m, data = rmedsem::mchoice, method = "lms")

  # mediated moderation
  rmedsem(est, indep = "smv:OwnLook", med = "SelfEst", dep = "MentWell")

  # moderated mediation
  rmedsem(est, indep = "OwnLook", med = "SelfEst", dep = "MentWell",
          moderator = "smv")
}

## blavaan
if (requireNamespace("blavaan", quietly = TRUE)) {
  # blavaan's fitting functions need the package to be attached
  library(blavaan)
  # short single chain to keep the example fast; use more chains and
  # iterations in practice
  bmod <- bsem(mod.txt, data = rmedsem::hsbdemo, n.chains = 1,
               burnin = 500, sample = 500, seed = 1,
               bcontrol = list(refresh = 0))
  rmedsem(bmod, indep = "math", med = "read", dep = "science")

  # highest density intervals instead of equal-tailed intervals
  if (requireNamespace("HDInterval", quietly = TRUE))
    rmedsem(bmod, indep = "math", med = "read", dep = "science", hdi = TRUE)
}
}

}
\references{
Baron, R. M., & Kenny, D. A. (1986). The moderator-mediator variable
distinction in social psychological research: Conceptual, strategic, and
statistical considerations. \emph{Journal of Personality and Social
Psychology}, 51(6), 1173--1182. \doi{10.1037/0022-3514.51.6.1173}

Lachowicz, M. J., Preacher, K. J., & Kelley, K. (2018). A novel measure of
effect size for mediation analysis. \emph{Psychological Methods}, 23(2),
244--261. \doi{10.1037/met0000165}

Zhao, X., Lynch, J. G., & Chen, Q. (2010). Reconsidering Baron and Kenny:
Myths and truths about mediation analysis. \emph{Journal of Consumer Research},
37(2), 197--206. \doi{10.1086/651257}
}
