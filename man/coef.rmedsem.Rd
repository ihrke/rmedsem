% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{coef.rmedsem}
\alias{coef.rmedsem}
\alias{confint.rmedsem}
\alias{nobs.rmedsem}
\title{Extract Effects from an rmedsem Object}
\usage{
\method{coef}{rmedsem}(object, method = NULL, ...)

\method{confint}{rmedsem}(object, parm, level = NULL, method = NULL, ...)

\method{nobs}{rmedsem}(object, ...)
}
\arguments{
\item{object}{the \code{rmedsem} object}

\item{method}{estimation method for the indirect effect, one of
\code{object$est.methods} (e.g., \code{"sobel"}, \code{"delta"}, \code{"montc"}, \code{"boot"} or
\code{"bayes"}); see Details for the default}

\item{...}{additional arguments (currently unused)}

\item{parm}{character vector; a subset of \code{c("indirect", "direct", "total")}}

\item{level}{the confidence level. The intervals are computed when calling
\code{\link[=rmedsem]{rmedsem()}} (argument \code{ci.two.tailed}), so \code{level} can only be used to
check that the stored intervals have the requested level.}
}
\value{
\code{coef()}: a named numeric vector with elements \code{indirect}, \code{direct}
and \code{total}. \code{confint()}: a matrix with one row per effect and columns
giving the lower and upper limits, labelled by their probabilities (e.g.,
\code{"2.5 \%"} and \code{"97.5 \%"}) or, for highest density intervals
(\code{rmedsem(..., hdi = TRUE)} for \code{blavaan} models), \code{"lower"} and
\code{"upper"}. \code{nobs()}: an integer.
}
\description{
\code{coef()} returns the estimated indirect, direct and total effects,
\code{confint()} their confidence (or, for Bayesian models, credible) intervals,
and \code{nobs()} the number of observations used to fit the model.
}
\details{
The indirect effect is estimated with several methods (see
\code{object$est.methods}), which all give the same point estimate
(the product of coefficients; for Monte-Carlo and Bayesian estimation the
mean of the samples) but different standard errors and intervals. By
default, the method is used that also underlies the Zhao, Lynch & Chen
approach: \code{"montc"} (Monte-Carlo) for \code{lavaan} and \code{modsem}, \code{"boot"}
(bootstrap) for \code{cSEM} and \code{"bayes"} for \code{blavaan}.
}
\examples{
mod.txt <- "
read ~ math
science ~ read + math
"
mod <- lavaan::sem(mod.txt, data=rmedsem::hsbdemo)
out <- rmedsem(mod, indep="math", med="read", dep="science")
coef(out)
confint(out)
confint(out, parm="indirect", method="sobel")
nobs(out)

}
